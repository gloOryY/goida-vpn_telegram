{
  "name": "@goida-vpn/telegram-bot",
  "version": "1.0.0",
  "scripts": {
    "dev": "ts-node-dev --respawn --transpile-only src/index.ts",
    "build": "tsc",
    "start": "node dist/index.js",
    "test": "jest --passWithNoTests"
  },
  "dependencies": {
    "@goida-vpn/shared": "*",
    "dotenv": "^17.3.1",
    "telegraf": "^4.16.3"
  },
  "devDependencies": {
    "ts-node-dev": "^2.0.0"
  }
}
