{
  "name": "@goida-vpn/auth-service",
  "version": "1.0.0",
  "main": "src/index.ts",
  "scripts": {
    "dev": "ts-node-dev --respawn --transpile-only src/index.ts",
    "build": "tsc",
    "start": "node dist/index.js",
    "test": "jest --passWithNoTests",
    "test:coverage": "jest --coverage"
  },
  "dependencies": {
    "@fastify/jwt": "^8.0.1",
    "@goida-vpn/shared": "*",
    "dotenv": "^17.3.1",
    "fastify": "^4.28.1",
    "pg": "^8.12.0",
    "postgres": "^3.4.4"
  },
  "devDependencies": {
    "@types/pg": "^8.11.6",
    "ts-node-dev": "^2.0.0"
  }
}
