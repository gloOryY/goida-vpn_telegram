{
  "name": "@goida-vpn/api-gateway",
  "version": "1.0.0",
  "scripts": {
    "dev": "ts-node-dev --respawn --transpile-only src/index.ts",
    "build": "tsc",
    "start": "node dist/index.js",
    "test": "jest --passWithNoTests"
  },
  "dependencies": {
    "@fastify/jwt": "^8.0.1",
    "@fastify/rate-limit": "^9.1.0",
    "@goida-vpn/shared": "*",
    "dotenv": "^17.3.1",
    "fastify": "^4.28.1"
  },
  "devDependencies": {
    "ts-node-dev": "^2.0.0"
  }
}
